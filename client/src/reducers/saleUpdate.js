//
//
// export default (state = , action) => {
//   switch (action.type) {
//     case "SET_EDIT_SALE":
//       return state.map(sale => sale.id === action.sale.id ? action.sale : sale)
//   }
// }
